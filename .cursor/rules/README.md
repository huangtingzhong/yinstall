# 项目 Cursor 规则（本仓库专用）

本目录下的 `.mdc` 为 **yasinstaller 仓库项目规则**，已纳入 git 跟踪。

- Cursor 打开本仓库时会自动加载 `.cursor/rules/*.mdc`。
- **禁止**将此处规则复制到用户全局目录（如 `~/.cursor/rules`）或 Cursor **Settings → Rules** 的全局规则库；否则易与其他项目冲突，且无法随仓库版本同步。
- 规范正文与 `CLAUDE.md` § Development Workflow 须保持一致；修改时**两处同改**。
