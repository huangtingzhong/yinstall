---
description: yinstall 开发流程、文件组织、测试环境与编码约束（BUG/功能须先方案后实现）
alwaysApply: true
---

# yinstall 开发规范

## 规范维护（必读）

本规范为 **yasinstaller 仓库项目规则**，仅存放在本仓库 **`.cursor/rules/`**（随 git 提交），**不是** Cursor 用户全局规则库。

- **禁止**复制到 `~/.cursor/rules` 或 Cursor Settings → Rules 的全局规则。
- 克隆/拉取本仓库后，在 Cursor 中打开项目即可生效；无需在全局再配一份。

本规范存在**两份副本**，内容须始终一致：

| 文件 | 用途 |
|------|------|
| **本文件** `.cursor/rules/yinstall-development.mdc` | Cursor 项目规则（`alwaysApply`） |
| **`CLAUDE.md`** 中 **Development Workflow (Mandatory)** | Claude Code / 无 Cursor 时的同一规范 |

**以后任何规范变更（工作流、测试、文件组织、测试机等）必须同时改上述两处。** 只改一处视为未完成。

---

## 强制工作流

### BUG 修复 / 功能新增

1. **先方案，后代码**：说明根因/需求、改动范围、影响步骤、风险与验证。**用户消息已含明确实现指令**（如「直接改」「合并到 xxx.go」「执行方案 B」）视为已同意，可跳过单独方案步骤。
2. **验证**（合并前至少执行）：
   ```bash
   go fmt ./...
   go vet ./...
   go test ./... -count=1          # 仓库内既有测试（internal/ 等）
   go test ./tmp/... -count=1      # 本次临时验证（若有 tmp/*_test.go；无则省略）
   ```
3. **测试策略**：
   - 有纯逻辑/解析/步骤 mock 可测时，编写测试并跑通；**trivial 断言可省略**。
   - **临时验证**：测试文件只写在仓库根 **`tmp/`**（如 `tmp/c031_profile_test.go`），`package xxx_test` + `import "github.com/yinstall/..."`；**禁止**在 `internal/`、`cmd/` 等代码目录新增临时 `*_test.go` / `*_temp_test.go`。
   - `tmp/` 已 **gitignore**，默认不提交；`go test ./tmp/...` 通过后**删除本次 `tmp/` 内文件**（保留 `tmp/.gitkeep`）。
   - **既有代码目录内 `*_test.go`**：仅当用户明确要求长期回归时增补并保留；勿为一次性验证在 `internal/` 下新建测试。
4. **端到端**：在真实环境用 `build/yinstall_*` 做 `-s` 单步或最小路径验证（见下文测试机）。
5. **Git**：默认不 commit / PR / 提交 `build/` 二进制，除非用户明确要求。

---

## 范围与文件组织

### 范围

- **最小 diff**，不顺手改无关文件。
- **DRY**：同一逻辑只实现一处；**第二处**需求出现后再抽到 `internal/common/*` 或同域已有 util 文件。

### 何时新建 `.go`

| 允许 | 禁止 |
|------|------|
| **新增步骤**：`c0xx_*.go` / `b0xx_*.go` 等 + `registry.go` 注册 | 单次 bugfix / 小功能新建 `*_helpers.go`、`*_extra.go` |
| **跨 2+ 步骤或跨包**且已有第二处调用点 → `internal/common/*` 或同域 `*_util.go` | 仅本步骤、≤30 行 helper 单独建文件 → **内联** |
| **重构拆分**：见下文「文件体量」 | 实现完成后仍留一次性碎片文件 → **合并回同域主文件** |

**DRY / debug 抽公共逻辑时**：优先写入同域**已有**文件（如 `yasboot_cmd.go`、`logger.go`、`*_util.go`、`debug_log.go`）；职责明显不同或目标文件已过大时再新建。

### 文件体量

| 行数 | 策略 |
|------|------|
| **&lt;500** | 直接改 |
| **500–800** | 可改；新增 &gt;50 行时考虑拆到同域 util，勿堆在一个步骤文件 |
| **≥800** | 优先小补丁；若必须加大量逻辑 → **先重构拆分**（拆分/refactor 不算「小功能新建」） |
| **~1000** | 避免继续堆叠；先拆再改 |

---

## 测试环境

控制端 macOS/Linux SSH 到目标机；SSH 用户 `root` / `yashan`（免密）；产品用户默认 `--os-user yashan`。

| 场景 | 主机 | 用法 |
|------|------|------|
| **单机** | `10.10.10.130` (aarch64) | `db`/`os`/`ycm`/`ymp`/`collect`/`stressos` `-t 10.10.10.130` |
| **YAC 多节点** | `10.10.10.125`, `10.10.10.126`（可扩展） | `db`/`os` 多 `-t`；`stressos --net` 节点互 ping + iperf3 |
| **YAC 单节点** | 如 `10.10.10.125` + `--yac` | `--yac-access-mode direct`、`--yac-exclude-disks` 等 |

```bash
make build-current
./build/yinstall_$(uname -s | tr A-Z a-z)_$(uname -m) db -t 10.10.10.130 --precheck -s C-007
```

- 日志：`--log-dir` 默认 `~/.yinstall/logs`；命名 `yinstall_<type>_<ts>.log` / `yinstall_<type>_debug_<ts>.log`
- collect/stressos 归档：`./output/<collect|stress>/<时间戳>/`

---

## 架构要点

```
cmd/yinstall/          internal/cli/          internal/runner/
internal/ssh/          internal/logging/      internal/common/
internal/steps/<域>/   # 一步一文件 + registry.go
```

- 步骤：`PreCheck` →（dry-run/precheck 跳过 Action）→ `Action` → `PostCheck`；失败 `LogErrorExit`（脱敏）。
- 上下文：`StepContext`（`Executor`、`Logger`、`Params`、`Results`、`TargetHosts`）。
- YAC：`ctx.HostsToRun()`、`ctx.ForHost(th)`；SQL/上传类常只跑首节点。
- 步骤 ID：B- OS | C- DB | E- standby | G- YCM | H- YMP | R- collect | S- stressos
- CLI 子命令：`os` `db` `ycm` `ymp` `standby` `clean` `collect` `stressos`
- 新增步骤：新建步骤文件 → `registry.go` 注册 → CLI `build*Params` 注入 `Params`

**API 速查**（函数表、Params 键、phase 命名）：见 `installer.md`；collect/stressos 见各包 `*_util.go`。开发强制流程见 **`CLAUDE.md` § Development Workflow**（与本文件同步，**改规范须两处同改**）。

---

## 编码约束（摘要）

### 必须复用的入口

| 场景 | 入口 |
|------|------|
| 远端命令 | `ctx.Execute` / `ctx.ExecuteWithCheck` |
| 产品用户 | `commonos.ExecuteAsUserWithCheck` / `ExecuteAsUserWithEnvCheckCtx` |
| SQL sysdba | `commonsql.ExecuteSQLAsSysdbaCtx` |
| 上传 | `ctx.Executor.Upload(..., ctx.UploadContext())`；安装包 `file.FindAndDistribute` |
| 路径删除 | `commonos.ValidateDeletePath` + `DeletePathUnder` |
| 多行 shell/SQL | collect/stressos 域内 `*RunShell`/`*RunSQL`；安装域经 Execute + `LogScriptPreview` |

**禁止**：步骤内手写 scp、重复拼 yasql、裸 `Executor.Execute`（框架层除外）、`fmt.Print` 绕过 Logger。

### Debug 日志（新改步骤须满足）

- 命令经 `Execute*` 封装 → debug 有 `>>> cmd`、exit、stdout/stderr。
- 多行脚本/SQL 执行前 `LogScriptPreview`。
- 多子任务步骤：`phase=plan` + 各项 `*-start`/`*-done`（collect/stressos 已有封装；安装域用 `ctx.LogPhase` 或包内 `debug_log.go`）。
- 终端：步骤起止 + 失败摘要；**不进终端**：phase 正文、完整 stdout、上传进度百分比。
- 密码等经 `logging.redact` 脱敏（可用 `--log-redact` 类开关，见 CLI）。

### 可选步骤

- 不需要执行：PreCheck 失败 + `Optional: true`。
- dry-run/precheck 缺上游产物：`runner.SkipPrecheckDryRunWhenUpstreamArtifactMissing`（db 包有 db 别名）。

---

## 新增功能检查清单

- [ ] 方案已获同意（或用户已明确指令）
- [ ] 步骤 ID / registry / CLI Params 正确
- [ ] 未为单次小改动新建 `.go`（新步骤、跨 2+ 复用、重构拆分除外）
- [ ] 复用 common/runner/域内 util；文件体量合理
- [ ] 上传、SQL、debug phase 符合上文摘要
- [ ] `go fmt` / `go vet` / `go test ./...`（及 `./tmp/...` 若有）通过；`tmp/` 内本次临时测试已删
- [ ] 真机最小验证：单机 130；YAC 改动另测 125/126（如适用）

---

## 构建

```bash
make build-current
go test ./... -count=1
# 临时验证（文件在 tmp/ 时）：
go test ./tmp/... -count=1
```
